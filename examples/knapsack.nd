let max a b = if a > b then a else b

let knapsack cap items =
  (fold t = array (cap + 1) 0
   | (wt, vl) <- items, w <- [cap, cap - 1..wt]
   -> t[w] := max t[w] (t[w - wt] + vl))[cap]

let result = knapsack 5 [(2, 3); (3, 4); (4, 5); (5, 6)]
